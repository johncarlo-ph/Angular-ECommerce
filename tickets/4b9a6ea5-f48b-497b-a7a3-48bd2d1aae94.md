I'll implement the changes according to the design document. Let me start by examining the current codebase structure.

<rewrite_file>
<path>index.html</path>
<diff>
--- index.html
+++ index.html
@@ -4,7 +4,7 @@
     <meta charset="UTF-8" />
     <link rel="icon" type="image/svg+xml" href="/vite.svg" />
     <meta name="viewport" content="width=device-width, initial-scale=1.0" />
-    <title>Vite + React</title>
+    <title>JC Merchandise</title>
   </head>
   <body>
     <div id="root"></div>
</diff>
</rewrite_file>

<rewrite_file>
<path>src/components/Navbar.jsx</path>
<diff>
--- src/components/Navbar.jsx
+++ src/components/Navbar.jsx
@@ -6,7 +6,7 @@
   return (
     <nav className="bg-blue-600 text-white p-4">
       <div className="container mx-auto flex justify-between items-center">
-        <Link to="/" className="text-xl font-bold">My Shop</Link>
+        <Link to="/" className="text-xl font-bold">JC Merchandise</Link>
         <div className="space-x-4">
           <Link to="/" className="hover:text-blue-200">Home</Link>
           <Link to="/products" className="hover:text-blue-200">Products</Link>
</diff>
</rewrite_file>

<rewrite_file>
<path>package.json</path>
<diff>
--- package.json
+++ package.json
@@ -1,5 +1,5 @@
 {
-  "name": "my-shop",
+  "name": "jc-merchandise",
   "private": true,
   "version": "0.0.0",
   "type": "module",
</diff>
</rewrite_file>

<create_file>
<path>src/components/Footer.jsx</path>
<content>
function Footer() {
  const currentYear = new Date().getFullYear();
  const shopUrl = window.location.origin;

  return (
    <footer className="bg-blue-600 text-white p-4 mt-auto">
      <div className="container mx-auto text-center">
        <p className="text-sm">
          Shop URL: <a href="/" className="hover:text-blue-200 underline">{shopUrl}</a>
        </p>
        <p className="text-sm mt-1">
          © {currentYear} JC Merchandise. All rights reserved.
        </p>
      </div>
    </footer>
  );
}

export default Footer;
</content>
</rewrite_file>

<rewrite_file>
<path>src/App.jsx</path>
<diff>
--- src/App.jsx
+++ src/App.jsx
@@ -1,5 +1,6 @@
 import { Outlet } from 'react-router-dom';
 import Navbar from './components/Navbar';
+import Footer from './components/Footer';
 
 function App() {
   return (
@@ -8,6 +9,7 @@
       <main className="flex-grow container mx-auto px-4 py-8">
         <Outlet />
       </main>
+      <Footer />
     </div>
   );
 }
</diff>
</rewrite_file>

<create_file>
<path>src/components/Footer.test.jsx</path>
<content>
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { render, screen } from '@testing-library/react';
import Footer from './Footer';

describe('Footer', () => {
  let originalLocation;

  beforeEach(() => {
    originalLocation = window.location;
    delete window.location;
    window.location = { origin: 'http://localhost:3000' };